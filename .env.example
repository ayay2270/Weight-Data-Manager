# SQLite on the central server (default)
DATABASE_URL=sqlite:///data/weight_manager.db
# Future PostgreSQL example:
# DATABASE_URL=postgresql+psycopg://user:password@db-host/weight_manager
POLL_SECONDS=7

